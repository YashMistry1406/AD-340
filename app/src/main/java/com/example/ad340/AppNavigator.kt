package com.example.ad340

//interface AppNavigator {
//    fun navigateToCurrentForecast(zipcode:String) /*we need to have a class that implements this and defines
//                                                    this behaviour if we call this method we navigate to our current forecast
//                                                    in  this case it the main activity so we add it to the main activity */
//    fun navigateToLocationEntry()
//
//    fun navigateToForecastDetails(forecast: DailyForecast)
//
//}